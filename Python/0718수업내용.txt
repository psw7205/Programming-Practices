// Python과 MySQL 연동

준비사항 - pymysql 외부모듈

# 0) MySQL이 미리 설치되어 있는지 확인
# 1) 파이참에서
#   [File]-[Settings]
# 2) Project ~ 카테고리 클릭
# 3) [Project Interpreter] 클릭
# 4) [+] 클릭
#    PyMySQL 검색후 설치
# 5) 공유폴더에서 mySQL_test.py
#   다운로드후 작업폴더에 붙이기
#   실행해서 데이타가 잘 나오는지 확인

// 클라이언트단은?
- HTML
- CSS

/ HTML 버전
- 웹페이지의 컨텐츠(이미지, 텍스트, 하이퍼링크, 테이블...)의 마크업언어
- HTML 4
- XHTML
- HTML 5

// 파이참에서 HTML5 문서 만들기
1) 폴더 마우스 우측 버튼
2) HTML 문서
3) HTML5

// HTML5 태그 특징
- 대소문자는 상관없다. 소문자 권장
- 구조
  <태그명> .... </태그명> : 짝이 있는 구조
  <태그명 /> : 단독 태그

- 태그명안에 들어가는 요소
  <태그명 [속성='값']>

  <a href='http://www.google.com'>구글로 이동하기</a>

// HTML5 태그
주석 <!-- 주석내용 -->
h1 ~ h6
 : 글자가 진하게
 : 숫자가 클수록 글자가 작아진다.
 : heading

p : 문단생성
  : paragraph

ul>li : 목록문단
ol>li : 순서목록문단

ul: unordered list
ol: ordered list
li : list

dl>dt+dd : 정의문단
dl : definition list
dt : definition title
dd : definition datat




// 파이참에서 html 코딩 기법
태그명 입력후 [Tab]

// Emmet 코딩 방식으로 HTML 입력하기
태그명1+태그명2... [TAB]
태그명*3 [TAB]  반복
$ 자동숫자증감
{} 내용입력
() 그룹핑












